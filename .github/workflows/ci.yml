name: CI

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  quality-checks:
    name: Quality checks (required)
    uses: ./.github/workflows/quality-checks.yml